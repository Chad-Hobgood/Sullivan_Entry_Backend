# Equipment Logging

`Calculate_Equipment_Usage.js` reads configured options from `Equipment Usage!B1`, counts recorded values in `B2:B`, includes unused options with zero counts, and writes to `Dashboard_Data_Link!AL:AM`. The function runs from `runLoggingUpdates()`.

See the [metric reference](../../../docs/METRIC_REFERENCE.md) for the output contract.
